// This file is designed to return a list of filepaths,
// relative to the 'unit' directory, without extensions,
// to be processed as unit tests

define(
    [   // dependencies...
        // 'foo/bar'
    ],
    function () {
        return [
            'urls'
        ];
    }
)
